а
Насколько я понимаю, переменная a объявляется, и по этому становится свойством window. Соответственно, условие не будет выполняться (с учётом инверсии) значит a == undefined.
       
b
Пример выдаст ошибку, т.к. переменная a не была объявлена. Объявлена только функция a. Как, впрочем,  и х.
       
с
Хотел сказать что выдаст undefined, т.к. переменной "a" ничего присвоели. Но при тесте выдало текст функции:
function a(x) {
    return x * 2;
}
Понять не могу, в чем дело.

d
Результат будет 10, т.к. в качестве третьего аргумента вводится число 3, затем ему же присваивается число 10, и выводится в alert.

e
Результат будет объект window, т.к. предполагаю, что если контекст неопределен, то поиск этого контекста добирается до последнего объекта window.
